var InfiniteScroller = InfiniteScroller || {};

// assign the Boot parameter to a function

// change the prototype of InfinitieScroller to call the Boot 
InfiniteScroller.Boot.prototype = {
  // pre load assets, think loading screen

  // create assets
}
